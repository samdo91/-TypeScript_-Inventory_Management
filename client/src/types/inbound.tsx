export type InboundTY = {
  productCode: number;
  productName: string;
  vendorCode: string;
  vendorName: string;
  quantity: number;
  purchasePrice: number;
  totalAmount: number;
};
export type InboundsTY = {
  product_id: string;
  BusinessPartner_id: string;
  quantity: number;
  totalAmount: number;
};
